"""The source the agent must fix. `greeting()` is intentionally wrong — the verifier runs it and
fails until it returns exactly "Hello, loopeng!". The agent may edit ONLY this file
(loop.yaml `limits.allowed_paths`)."""


def greeting() -> str:
    return "TODO"
